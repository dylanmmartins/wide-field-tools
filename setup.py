import setuptools

setuptools.setup(
    name = 'wfTools',
    packages = setuptools.find_packages(),
    description = 'Preprocess wide-field imaging data.',
    author = 'Dylan Martins',
    version = 1.0
)